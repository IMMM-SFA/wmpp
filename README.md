# WM Post Processing (wmpp)

`wmpp` (in development) produces Water Scarcity GIF and PLEXOS inputs from WM simulation results.

## Installation

You can install the wmpp using devtools:

``` r
devtools::install_github("IMMM-SFA/wmpp")
```
